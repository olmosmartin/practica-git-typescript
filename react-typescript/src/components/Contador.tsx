import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Contador({}: Props): ReactElement {
    return (
        <div>
            contador
        </div>
    )
}
